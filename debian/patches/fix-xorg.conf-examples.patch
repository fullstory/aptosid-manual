--- a/bg/hw-dev-hw-dri-bg.htm
+++ b/bg/hw-dev-hw-dri-bg.htm
@@ -263,7 +263,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/da/hw-dev-hw-dri-da.htm
+++ b/da/hw-dev-hw-dri-da.htm
@@ -261,7 +261,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/de/hw-dev-hw-dri-de.htm
+++ b/de/hw-dev-hw-dri-de.htm
@@ -263,7 +263,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>Bei Verwendung von mehr als einer Grafikkarte muss die PCI bestimmt und in xorg.conf eingefügt werden:</p>
 <pre>
--- a/el/hw-dev-hw-dri-el.htm
+++ b/el/hw-dev-hw-dri-el.htm
@@ -276,7 +276,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<!--p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p-->
 							<p>Εάν έχετε περισσότερες από μια κάρτες, θα πρέπει να βρείτε το PCI και να το συμπεριλάβεται στο xorg.conf:</p>
--- a/en/hw-dev-hw-dri-en.htm
+++ b/en/hw-dev-hw-dri-en.htm
@@ -262,7 +262,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/es/hw-dev-hw-dri-es.htm
+++ b/es/hw-dev-hw-dri-es.htm
@@ -262,7 +262,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>Si tiene más de una tarjeta gráfica, necesitará determinar la PCI e incluirla en el xorg.conf:</p>
 <pre>
--- a/fr/hw-dev-hw-dri-fr.htm
+++ b/fr/hw-dev-hw-dri-fr.htm
@@ -264,7 +264,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/hr/hw-dev-hw-dri-hr.htm
+++ b/hr/hw-dev-hw-dri-hr.htm
@@ -261,7 +261,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/hu/hw-dev-hw-dri-hu.htm
+++ b/hu/hw-dev-hw-dri-hu.htm
@@ -262,7 +262,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/it/hw-dev-hw-dri-it.htm
+++ b/it/hw-dev-hw-dri-it.htm
@@ -263,7 +263,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>Se si possiede più di una scheda grafica si deve scoprire qual è il suo indirizzo PCI ed includerlo nel file xorg.conf:</p>
 <pre>
--- a/ja/hw-dev-hw-dri-ja.htm
+++ b/ja/hw-dev-hw-dri-ja.htm
@@ -283,7 +283,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/nl/hw-dev-hw-dri-nl.htm
+++ b/nl/hw-dev-hw-dri-nl.htm
@@ -261,7 +261,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/pl/hw-dev-hw-dri-pl.htm
+++ b/pl/hw-dev-hw-dri-pl.htm
@@ -262,7 +262,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/pt-br/hw-dev-hw-dri-pt-br.htm
+++ b/pt-br/hw-dev-hw-dri-pt-br.htm
@@ -262,7 +262,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
--- a/ro/hw-dev-hw-dri-ro.htm
+++ b/ro/hw-dev-hw-dri-ro.htm
@@ -262,7 +262,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>Dacă aveți mai mult de o placă grafică va trebui să descoperiți portul PCI și s-o includeți în the xorg.conf:</p>
 <pre>
--- a/ru/hw-dev-hw-dri-ru.htm
+++ b/ru/hw-dev-hw-dri-ru.htm
@@ -262,7 +262,7 @@ mv /root/xorg.conf.new /etc/X11/xorg.con
 Section "Device"
          Identifier      "Device 0"
          Driver          "nvidia"
- EndSection
+EndSection
 </pre>
 							<p>If you have more than one graphics card you will need ascertain the PCI and to include it in the xorg.conf:</p>
 <pre>
